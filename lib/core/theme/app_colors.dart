import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color scaffoldBackground = Color(0xff1f2029);
  static const Color surface = Color(0xff17181f);
  static const Color card = Color(0xff1f2029);

  static const Color primary = Colors.deepOrangeAccent;
  static const Color primaryDark = Colors.deepOrange;

  static const Color white = Colors.white;
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white54;
  static const Color textMuted = Colors.white60;

  static const Color transparent = Colors.transparent;
}